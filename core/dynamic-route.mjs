'use strict'

/**
 * 生成动态路由的一些必要核心函数
 */
import { POSTS_FOLDER_NAME } from './dynamic-route-constants'

/**
 * 获取指定文件夹的动态路由
 * @param {string} routeName 路由名称
 * @param {string} locale 语言
 * @returns {string} 路由的path
 */
export async function getDynamicRouter() {
    // TODO: implement
    // 获取指定文件夹下的所有 md 文件
    // const mdFiles = await getMarkdownFiles(
    //   process.cwd(),
    //   POSTS_FOLDER_NAME
    // )
}
